version: "3.9"

services:
  rag-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data          # persist FAISS index
      - ./credentials:/app/credentials  # mount service-account key
    env_file:
      - .env
    restart: unless-stopped